{
  "name": "poke_api",
  "version": "1.0.0",
  "description": "Rest API nodeJS (Pokemon Catch)",
  "main": "index.js",
  "scripts": {
    
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd poke_web && yarn run build",
    "install-client": "cd poke_web && yarn install",
    "heroku-postbuild": "yarn run install-client && yarn run build",
    "client": "cd poke_web && yarn start",
    "start": "yarn run nodemon index.js"
  },
  "author": "Jallu Ramadhan",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.13",
    "nodemon": "^2.0.6"
  }
}
